# Isolated method-signature planning prerequisite

Status: bounded implementation ready for independent review; no native entry/return integration or timing-class admission.

Base compiler: `2a5a57a9d558df4846ccc96fbb53e5a59f1f6b1f`. This package adds one production module, `src/emit/native-method-signatures.ts`, and its own focused tests only. No existing emitter, callable, lexical, typed-local, package/public export, engine, pin or canonical file changes. The user selected modern GSAP while this isolated task was finishing; further legacy timing prerequisite work is not part of this handoff.

The planner requires exact source plus authenticated complete Class and instance XML, reconciles complete reflection rather than a partial count-only projection, and produces source-ordered fixed signatures with Number/Boolean/wildcard parameters, exact literal defaults, min/max/formal length, normal Number/Boolean/void/wildcard return ownership, and distinct throw spans. It requires an Object-root declaration and rejects whole unsupported declarations. No runtime execution/coercion/publication occurs.

The existing `nativeCallableMetadata` parameter-count data cannot establish typed parameter authority. Integration must explicitly obtain the additional full XML evidence from toolkit receipts. The focused README contains the internal API and the required future entry/storage/return integration order; those files remain parent-owned and unwired.

Validation: a fresh worktree at the exact base applied `provider.patch`, authenticated all 137 delivery files, built with the repository's existing TypeScript compiler and passed 611 assertions. The assertions include fixture/receipt authentication, all seven repeated original groups, complete positive plans for EntrySubject/ParameterStorage/PlainSubject/PlanSubject, source offset and structural-node ownership, metadata/default/namespace negatives, and complete holds. All installed original compiler/player tools were checked against retained receipts. Final verification is under `verification/`, and exact replay is `replay-final/`.

Original evidence: 62 + prior 43 + prior 22 + seven new observations = 134 retained observations, not 134 native matches. The seven new repeated PlanSubject observations cover thrown primitive/object identity, wildcard defaults/supplied undefined, negative-zero default/supplied positive zero, and a commented return prefix. Complete source, wrapper, raw Class/instance XML, SWF, commands, capture tools and receipts are retained. ReturnSubject and OverrideChild remain whole holds. Wider legacy method entry/return/arity evidence is retained without relabeling it as compiler admission.

Boundary details: legacy parser throw nodes share RETURN kind, so original keyword ownership separates them. Signed literal nodes have reversed bounds, so only an exact adjacent sign and original operand span are recovered; tests verify the complete negative-zero parameter span. Typed try/catch/finally, derived overrides, accessors, arguments/rest/nested functions, unsupported signatures/defaults/namespaces, extra declarations and broad typed fallthrough remain held. ParameterStorage's catch is permitted only for its wildcard return signature; this is not proof of its future storage lowering. Reflection integrity hashes are not a trust root and must be supplied by authenticated toolkit evidence.

No browser/native emission tests apply to this data-only module. The captured Flash observations were repeated, and compiler execution was checked in Node with an actual full TypeScript build. Whole game readiness, GSAP behavior, typed invocation and return execution remain unclaimed.
