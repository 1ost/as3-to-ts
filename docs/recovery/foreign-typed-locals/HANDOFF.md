# Frozen generic foreign typed-local compiler candidate

The implementation is ready for independent review. Integration remains the parent's decision under the user's newly approved modern GSAP direction. No additional legacy GreenSock capability is implemented or scheduled by this packet.

Base compiler: `2a5a57a9d558df4846ccc96fbb53e5a59f1f6b1f`. Final proof provider: `cc1baecaa0c2b4dd701516bc9a5d5c86188004fd`. There are four production files and 155 total delivery files. The candidate extends the existing exact declaration-domain capability; no engine or shared pin changes are included.

Production changes are limited to annotation/reference forwarding in native-declaration-plan.ts and native-lexical-members.ts, nominal local storage in native-typed-locals.ts, and token/import forwarding in native-callable-classes.ts. Domain validation remains complete and immutable before capability publication. Foreign coercion uses the same token as Class publication and performs no lazy Class read. Unused source declaration imports are erased after their local annotations become tokens, avoiding duplicate TypeScript bindings in the captured qualified namesake case; surviving source value/type identifiers retain imports. General emitter.ts is unchanged.

The complete new 12-class Object-root fixture is separate from the retained original domain. Its 41 nominal/storage, seven initialization and nine Error observations repeat in Flash and match in Node/Chromium for ES5/ES2015 consumers: 57 rows total, ten runtime controls, four comparison negatives, 754 actual provider type files and zero type diagnostics. Constructor-local default/read-before-declaration/coercion cases are included. Initial 54-row new captures are retained unchanged; they are not source replacements for the final 57-row run.

Twenty-eight compiler/operation guards pass. Both whole-group diagnostics are preserved: the original complete 12-class domain with all 33/7/14 observations remains ancestry-held, and the intact five-source timing domain is held at unresolved Dictionary declaration identity. No original classes or methods were dropped to claim readiness. The old domain suite's seven isolated class maps still reject missing target declarations/ancestry; only their expected guard category changed to match complete annotation planning.

Nine retained regression suites pass: declaration-domain runtime, self constructors, typed locals, typed lexical map, Array precedence, typed guards, declaration-order guards, declaration-domain guards and isolated-domain holds. Their historical provider pins remain explicit in their own reports. They are not relabeled as current cc1 proof.

`compiler.patch` reconstructs all 155 raw file hashes in `replay-compiler` from the pinned baseline with `core.autocrlf=false`; `delivery.zip` contains the same files. The fresh replay builds and passes all new runtime/type/guard/whole-group tests. Actual final artifacts are under `replay-compiler/.cache/native-foreign-typed-locals/runs/run-YSENuc`. `verification-files.json` and `verification.zip` freeze these results and declaration outputs. Main checkouts, user files and dependency pins were not edited.

The test runtime is explicitly an oracle adapter around intact generated classes. External observer/RHS callbacks use an existing trusted builtin script context solely for provider invocation; source closure compilation is not claimed. Error fields are observed through the real source property provider. These callbacks do not implement method bodies or mutate compiled locals. Deferred construction/order and captured same-domain/cross-domain identity checks execute real emitted classes and actual common providers.

Remaining restrictions include derived declarations, ordinary typed/void method signatures, source closures, unsupported catch rebinding, reference compounds/updates, source enumeration/destructuring and general CLI/bundler output ownership. The existing programmatic declaration-domain caller must reserve one resolved output per domain. This candidate neither completes the port nor establishes legacy timing readiness.
