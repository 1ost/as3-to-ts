# Original source relational lowering

The explicit nativeRelationalModule option selects the four public common AS3Relational helpers for pure original symbolic <, <=, > and >= nodes. Authenticated lazy callable source, complete source metadata and lexical dispatch are required. Missing context or unsafe module text rejects. Omitting the option preserves the previous route. This is compiler source consumption; no raw primitive helper or local compatibility provider is introduced.

Original RELATION sequences fold left, keeping operands in original order even for > and <=. The common helper reverses primitive conversion internally as required. Nested source expressions, short-circuit branches, source addition and assignments retain their own evaluation boundaries. Helpers are inserted before generated callable/local code and use source-collision-safe aliases. Two generated numeric arity comparisons remain raw per fixture class.

Complete retained76 and expressions14 source/capture pairs are copied byte-for-byte from research73fab80dd9e6364e154d286e77cef94437e9365c2a036d98cebe8de5e775c66c. The six new alias/comment/return-prefix observations also repeat with full source, SWF/tool receipts and complete Class/instance XML. All96 observations pass Node/Chromium with ES5/ES2015 consumers against committed engine4c3eaf32cefc654e8b086036705c5576b9b4edf4 actual declarations. Provider code uses its supported engine target; whole-engine ES5 admission is not claimed.

The complete mixed4 original pair remains held rather than selecting individual methods. AS2 aliases and mixed comparison/type/membership nodes reject in the new path. Four result-comparison negatives per fixture and23 compiler option/source-authority/unsupported controls protect the bounded scope. Original-to-helper counts are8,13 and7; two generated arity checks remain for each class. Date/XML/XMLList/foreign-host rejection is checked through genuinely emitted four-operator methods on both argument sides (56 held cases); these are not successful original outcomes or new type admission.

Set LAYA_ENGINE_REPOSITORY, PLAYWRIGHT_MODULE and physical PYTHON, build the compiler, then run npm run test:native-relational. The runner archives the exact engine commit and uses its actual provider declarations. Source closures, ordinary typed method signatures, foreign reference/derived source publication, broader mixed operators, Date/E4X semantics, Error/Class and full timing/game readiness remain separate prerequisites. Existing typed-local/addition suites keep their own historical engine pins.
