# Direct dynamic String.replace construction

Run node tests/native-generated-dynamic-patterns/run.cjs after npm run tsc. The complete DynamicReplace AS3 subject matches fourteen repeated AIR observations on ES5/ES2015 in Node and Chromium with zero type diagnostics. Five compiler guards retain direct builtin identity, arity and source receiver requirements.

A direct two-argument new RegExp inside a typed String.replace uses the shared engine pattern-construction provider. The pattern handle never escapes. General RegExp Class/constructor identity and other pattern forms remain separate capabilities. Full maintained StringUtil is retained by the AIR packet and is investigated separately.
