# Generated wildcard for-in

Run npm run test:native-generated-wildcard-forin after npm run tsc.

Both complete subjects from the engine generated-wildcard-forin AIR packet
are emitted unchanged. A separate observer compares all 17 repeated ADL rows in
Node/Chromium on ES5/ES2015, baseline and with reference/signature passes. Strict
generated/provider type checking must be clean. Seven rejection guards and three
comparison negative controls supplement the runtime observations.

Generated loops require a proven existing wildcard local or parameter and the
explicit nativeEnumeration/common AS3Property provider bindings. Keys come from
as3EnumerableKeys; the compiler retains their source numeric/string identity.
An explicit cursor loop preserves ES5 output, one receiver evaluation, empty-loop
slot preservation and source break/continue labels. It closes the common iterator
on abrupt completion. This is source lowering, not an application-local bridge.
The host observer creates its mutable destination with the common dynamic Object
factory; it does not weaken source object ownership checks.

String targets have separate AIR qualification in native-generated-string-forin.
Other typed, inline-declared, member and catch-shadow targets remain held. Mutation
during iteration and prototype/delegate ordering are not qualified by this
packet. The complete ObjectUtil methods and application still require validation.
