# Generated source-class is tests

Run `node tests/native-generated-source-is/run.cjs` with sibling LayaAir-op2.
The runner authenticates its generated-source-is AIR packet and emits all seven
complete subjects. Twelve rows compare source Class initialization and operand
ordering, null/undefined, exact and derived instances, unrelated values, lack of
conversion-hook calls and the owning Class target. Both ES5/ES2015 run in Node
and Chromium; generated and provider type diagnostics must be zero.

Four guards reject missing common type-test authority, Class-initializer tests
and shadowed Class/wildcard operands. Three corrupted comparisons check the
oracle comparator. Existing source and interface as fixtures remain regressions;
interface is tests and Class-initializer tests are not enabled by this change.
