# Generated single IOError catch

Run `node tests/native-generated-io-catch/run.cjs --combined` after building.
The engine generated-io-catch packet authenticates the complete AS3 Catcher and
thirty observations from two identical AIR runs. Both ES5/ES2015 outputs match
all rows in Node and Chromium, with zero generated/dependency type diagnostics.
The observer invokes generated methods; it contains no translated catch logic.

A catch must resolve to the exact native flash.errors.IOError declaration at its
authenticated source span, with an explicit provider binding and no lexical
shadowing. The engine's private allocation proof selects matches. Mismatches
rethrow the original value; normal JS finally semantics preserve the captured
return, rethrow and replacement order. Mutable public Error names do not select
catches. Three native host/prototype/lookalike guards reject forged errors.

Four compiler controls reject another typed Error, sibling catch clauses, a
shadowing parameter and an absent provider. Three comparison controls detect
altered expected rows. Existing single builtin Error catches retain their own
allocation predicate. Generated IOError descendants and broader catch dispatch
remain unqualified. This fixture does not establish SoundPlayer completion.
