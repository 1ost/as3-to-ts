# Planned source-class Vector specialization

Run `node tests/native-source-class-vectors/run.cjs --combined` after building.
The engine packet authenticates 56 AIR observations and all six complete subjects.
The compiler compares unchanged emitted subjects in Node/Chromium on ES5/ES2015,
with zero generated/provider type errors.

Declaration-token specializations publish after their nominal identities, without
loading source implementations. Only Vector construction reads the element Class;
annotations, null coercion and failed reference coercion preserve deferred static
initialization. Covariance retains the original Vector and its concrete element
write checks, including widening to an inherited interface.

Four compiler guards retain unresolved/nested elements, shadowed constructor
names and arbitrary length coercion. Three comparison controls reject changed
results. Provider checks cover constructor/declaration specialization identity,
same-name foreign domains, forged tokens/instances, hostile host proxies and
canonical interface ancestry. The observer does not replace source methods.

Native class publication, arbitrary constructor arguments, nested Vector targets,
and application class ancestry remain separate prerequisites.
