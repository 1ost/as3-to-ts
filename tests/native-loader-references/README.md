# Canonical loader references through generated source

Run `npm run tsc` then `node tests/native-loader-references/run.cjs`. The complete captured LoaderHolder is emitted by the production native Class module factory on ES5 and ES2015. Real Laya/WebGL Chromium compares all 80 authenticated AIR rows under script-src self, with zero generated/provider type errors.

Seventeen compiler guards reject missing/mismatched bindings, shadowed Class parameters and unauthenticated native ancestry. Eighteen runtime guards cover counterfeit instances, typed slot/parameter atomicity, proxy traps and independent source Classes across two domains. A Bitmap as-removal mutation must fail the prototype-forgery guard per target. Native allocations belong to the host fixture; no Node allocation, generated native subclass, loader I/O or full application completion is claimed.
