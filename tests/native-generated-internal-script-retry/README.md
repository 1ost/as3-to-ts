# Generated internal members across Class script retries

Run node tests/native-generated-internal-script-retry/run.cjs after npm run tsc.
The three complete, unchanged subjects come from the shared engine's authenticated
internal-script-retry AIR packet (engine evidence commit 7a2886440). All sixteen
observations match through production module emission and real ApplicationDomain
loading in Node and Chromium, for ES5 and ES2015, with zero type diagnostics.
The browser CSP forbids runtime compilation. Eleven rejection guards, five domain
checks and one applied Class-script identity mutation pass per target.
Report: .cache/native-generated-internal-script-retry/run-7ycCXn.

Retained failed instances and successful instances have independently mutable
internal uint storage, while methods, the Boolean getter and static uint constant
remain accessible to a same-package peer. The original thirteen retry observations
also match. Before this change the complete input hit the planner's internal retry
qualification guard. The common engine already preserves this behavior; no runtime
bridge change was needed.

Admission is limited to root Class retries. Derived retries in a package with
internal declarations remain rejected, including stable source-root ancestry.
Existing emitter guards still constrain supported internal trait shapes. This is
not a ZipFile module, complete startup, or gameplay pass.

Regressions: package domains 33 AIR + 12 construction rows (run-z8OZUi), root retries
13 rows (run-m9shEH), derived retries 16 rows (run-rx2xFE), all targets/runtimes.
The obsolete package-domain rejection of root internal retries is replaced with
rejection of an unknown Class-script selection; derived internal rejection remains
explicitly covered in this suite.
