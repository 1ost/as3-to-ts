package { import flash.display.Sprite; import flash.external.ExternalInterface; import init.Log; import init.Subject; public class InitOracle extends Sprite { public function InitOracle() {Log.rows.push("before-use");var cls:Class=Subject;Log.rows.push("after-class-value");var one:Subject=new Subject();var two:Subject=new Subject();Log.rows.push("after-two-instances");ExternalInterface.addCallback("snapshot",function():String{return encodeURIComponent(JSON.stringify(Log.rows));});} } }