# Generated Array constructor parameters

Run run.cjs with optional --combined. Both complete AIR subjects match all
11 retained observations in Node/Chromium on ES5/ES2015 with strict generated
and provider typechecking. Three guards reject non-null defaults; three negative
comparison controls cover missing/reordered/incorrect rows. Reports retain source,
observer and bundled provider hashes. The common property coercion provider and
captured Array identity perform entry conversion; no local compatibility shim.
