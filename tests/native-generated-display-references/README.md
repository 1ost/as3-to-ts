# Generated DisplayObject reference boundary

Run `node tests/native-generated-display-references/run.cjs` after `npm run tsc`.
The engine's generated-display-references packet authenticates 24 repeated AIR
observations across complete Reader, IMediator and maintained Mediator subjects.
Both ES5 and ES2015 outputs execute against real initialized Laya in Chromium.
The emitted/provider graph must type-check without diagnostics. There is no
mocked display provider or Node runtime pass claim.

The explicit nativeDisplayObjectReferenceModule option requires a generated
declaration/reference plan and matching DisplayObject import/provider binding.
Ten rejection controls cover absent/mismatched configuration, shadowed targets
and Class initializer operations. Prototype forgeries, constructor tags and
host proxy traps independently exercise the closed runtime identity boundary.
Three comparison controls check missing, reordered and modified AIR rows.

Only direct DisplayObject targets inside functions/accessors are qualified here.
Native construction, subclass entry, generalized native Class operands and full
EMVC/application integration remain outside this fixture.
