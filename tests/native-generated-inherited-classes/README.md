# Generated inherited Class and declaration identity

From the compiler root, run `node tests/native-generated-inherited-classes/run.cjs`
and optionally `--combined`, after `npm run tsc`. LAYA_ENGINE_REPOSITORY selects
the common engine checkout, defaulting to the sibling LayaAir-op2 worktree.

The runner authenticates nativeFlashOracle/generated-inherited-classes and emits
four complete unchanged subjects: the separately compiled parent and child
shared.Shared, child.Reader and child.Derived. All 47 AIR observations match on
ES5/ES2015 in Node and Chromium, including a child domain, same-domain load and
later child inheriting all published subjects. Strict generated and provider
type checking reports zero diagnostics. Eight compiler rejection guards, fifteen
runtime checks per target/runtime, three altered row comparisons and two runtime
mutations (missing inherited selection and missing Class-return coercion) protect
the comparison.

The declaration-plan option `inheritScriptClasses: true` requires an explicit
scriptDomainProvider and script globals for every planned class. The plan selects
each existing declaration through its genuine bound script cohort before local
token allocation. Existing declarations reuse the exact parent token; Class reads
resolve the original Class and skip child factory/script-unit publication. An
attempt to publish a child generation for an inherited declaration rejects.
Absent inherited declarations retain the ordinary local token and factory path.
The default plan behavior is unchanged. Interface cohorts and package-internal
alias membership remain held rather than mixing declaration authorities.

A derived source class registers against the actual parent's opaque inherited
lexical base reference. That reference only authorizes inheritance registration;
it cannot impersonate the parent's private-member capability. Existing nominal
ancestry checks remain enforced by the engine.

Generated typed Class returns now call the existing common as3CoerceClass helper,
as Class parameters already do. Return conversion preserves deferred finally
completion, including an overriding return, and rejects ordinary objects and
functions with source error #1034. Null/undefined and registered/builtin Class
values retain the captured source behavior. Missing Class-provider authority
remains a compiler error. Generic Class field storage is not admitted here.

The native observer supplies each cohort's domain and atomically publishes its
pending local headers using actual emitted tokens and lazy Class resolvers. It
does not replace subject method bodies. A module alias preserves the emitter's
normal same-package Derived import in the flat temporary harness layout.
These explicit host operations do not qualify source Loader/Sprite behavior or
automatic module discovery/activation. Source static initializers, interface and
package-internal collision selection, full ApplicationDomain semantics, native
asset loading and authenticated game flows remain separate requirements.
