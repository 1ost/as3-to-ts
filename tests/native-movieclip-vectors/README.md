# Canonical MovieClip Vector elements

Run `node tests/native-movieclip-vectors/run.cjs --combined` after building.
The complete unchanged Consumer matches 39 authenticated AIR observations on
ES5/ES2015 in Chromium with initialized Laya; there is no Node runtime claim.

Explicit `nativeVector: true` requires the qualified MovieClip provider and
canonical constructor proof. This authorizes Vector element identity, not
source subclass construction, arbitrary native elements or reflection.
Eight compiler guards retain unresolved/misbound providers, forged Vector
plans, shadowed names, nested Vectors and arbitrary constructor coercion.
Three comparison controls and runtime forged-class/prototype/proxy guards pass.

The observations include fixed-length source RangeError identity and message,
MovieClip/subclass insertion, Sprite/Shape rejection, nullish coercion, raw
assignment results, splice specialization and native/interface covariance.
Full Menu/controller execution and application display ancestry remain open.
