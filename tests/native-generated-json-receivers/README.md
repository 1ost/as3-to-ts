# Public receiver members with private-name collisions

Run `npm run tsc`, then
`node tests/native-generated-json-receivers/run.cjs --combined`.

Two complete subjects from the engine's authenticated generated-json-library
packet are emitted: maintained JSONToken and authored Reader. Five AIR rows
compare private/public value separation through a typed private field and
parameter, writes, null values and null-receiver TypeError 1009. Both ES5/ES2015
run in Node/Chromium with strict generated/dependency checks. Six guards retain
holds for unknown receiver types, compound writes, updates, deletes and calls;
three corrupted comparisons verify the comparator rejects changed output.

A public variable/accessor on a different planned source class uses common
property dispatch, rather than the current class's same-spelled lexical token.
Private field receiver identity comes from its resolved declaration occurrence;
identifier identity must resolve uniquely from its source type. This does not
admit arbitrary chained expressions or foreign nonpublic access. Full maintained
JSON-library emission and its other 34 observations remain separate work.
